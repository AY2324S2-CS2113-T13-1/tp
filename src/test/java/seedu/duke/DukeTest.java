package seedu.duke;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

/**
 * Represents a duke test.
 * A DukeTest object corresponds to a test that checks the correctness of the answer.
 */
public class DukeTest {
    @Test
    public void sampleTest() {
        assertTrue(true);
    }
}
